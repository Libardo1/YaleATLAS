from setuptools import setup
from setuptools import find_packages

setup(
    name='viz',
    version='0.0.1',
    description='Library 4 viz',
    install_requires=['numpy', 'matplotlib'],
    packages=find_packages()
)
